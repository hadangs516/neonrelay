# 검증 결과 — NEON RELAY v1.0

## 실행 결과

서버 로직 20개 + 게임 로직 21개 = 총 41개 모의 테스트 통과.

### 서버

- setup 실행 전 요청 차단, 재실행 시 데이터 유지
- 한글 계정 등록, 중복 이름·수식형 이름 거부
- PIN 0516 보존, 원문 PIN 미저장, 숫자형·세 자리 입력 거부
- 재로그인 시 이전 세션 무효화, 익명 시작 거부
- start 재요청 시 시작 횟수 중복 방지
- 누적 60초 기록, 중복·늦게 도착한 요청으로 중복 가산 방지
- 미래 시간·잘못된 점수·지나치게 이른 성공 기록 거부
- 정상 결과 저장, 같은 결과 재전송 시 XP 중복 방지
- 랭킹에서 인증 정보 제외, 로그아웃 후 토큰 거부
- PIN 실패 5회 후 잠금, 5분 후 복구

### 게임

- 최초 수동 기기 선택, 로그인 전 출격 불가
- 실제 Three.js 객체로 맵·플레이어·신호 3개 생성
- 키보드·터치 스틱 이동, 대시 무적·재충전
- 입력 초기화, 원형 맵 바깥 이동 차단
- 신호 3개 누적 충전·완료
- 레벨업 카드 선택·재개, 9종 업그레이드 적용
- 보스 생성·정지·성공 종료·결과 전송
- 6분 제한에 따른 미완료 종료
- 진행 중 시간 누적, 일시정지 중 시간 제외
- 강제 저장 실패 감지·재시도
- 연결 검증 만료 시 일시정지
- 아이폰 뷰포트 높이 계산
- 엔티티 수 상한

게임 테스트의 보스·종료 분기는 체력과 화력을 높인 테스트 상태로 실행했습니다. 이는 기능 검증이며 실제 난이도나 클리어 가능성, 재미에 대한 플레이 테스트를 대체하지 않습니다. 테스트용 상태 주입은 검증 스크립트 안에서만 수행하고 배포 HTML에는 포함하지 않았습니다.

## 실행 방법

Node.js가 있는 환경에서 저장소 루트 기준:

```bash
node source/tests/server.cjs
node source/tests/game.cjs
```

추가 의존성 설치가 필요하지 않습니다. 실제 Three.js 모듈과 메모리 안의 Google Apps Script·DOM·WebGL·네트워크 대체 객체를 사용합니다.

## 아직 검증되지 않은 항목

- 실제 Google Apps Script 배포·CORS 응답·시트 쓰기
- 실제 Chrome / Safari 화면 렌더링과 터치 동작
- 아이폰 안전 영역·키보드·주소창 변화의 실기기 동작
- 안드로이드 전체화면 허용 여부와 폴더블 화면 변화
- GPU 프레임률·발열·배터리·다수 사용자 동시접속
- 실플레이 난이도와 장시간 플레이의 재미
- 실제 카카오톡 링크 미리보기

테스트 브라우저가 작업 공간의 로컬 주소 접근을 차단하여 화면 검증을 진행하지 못했습니다. 통과 항목을 시각 테스트나 실제 구글 연결 성공으로 해석하면 안 됩니다.

## 배포 후 최소 확인

1. 새 계정을 PIN 0516으로 등록하고, 로그아웃 후 0516으로 재로그인합니다.
2. 1분 이상 플레이한 뒤 Players의 playSeconds 증가를 확인합니다.
3. 일시정지한 동안 시간이 늘지 않는지 확인합니다.
4. 라운드 종료 후 Runs와 랭킹을 확인합니다.
5. 모바일에서 스틱과 대시를 동시에 조작하고, 화면 회전·앱 전환 후 복귀를 확인합니다.
6. publish-helper로 실제 게임 주소를 설정한 후 카카오톡 공유 미리보기를 확인합니다.
